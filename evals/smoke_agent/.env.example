# Copy to .env for local live-eval runs (CI injects NVIDIA_API_KEY).
# Paths and model defaults: monkeybot_config/monkeybot.yaml

# Free key: https://build.nvidia.com — open any model → "Get API Key"
# NVIDIA_API_KEY=nvapi-your-key
