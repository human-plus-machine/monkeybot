# Internal (not shipped in OSS)
internal/

# Environment
.env
.env.*
!.env.example

# Local scaffolded harness config (defaults ship in src/monkeybot/monkeybot_config/)
/monkeybot_config/

# Demo agent MCP (may contain PATs; untrack with git rm --cached if already committed)
demo_agent/monkeybot_config/mcp.json

# Live eval run artifacts (only evals/baselines/ is versioned)
/evals/runs/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# uv
.venv/
.uv/

# Data (SQLite DB + memory files — never commit)
data/

# Testing & coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.deepeval/

# macOS
.DS_Store

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Docker
docker/data/

# Planning Documents
.prt/
.monkeymode/

# Reference clones (read-only)
code/

# Local venv for evals dependency experiments
.evals-venv/