"""Agent turn loop and streaming events.

Public entry: ``monkeybot.core.runtime.loop.run``. See that module's docstring
for the runtime package layout (``turn_loop``, ``tool_dispatch``, helpers).
"""
