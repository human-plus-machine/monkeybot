# User story: trustworthy first-run CLI demo

As a presenter, I can create a fresh `fake` agent and run `validate` and `doctor`
without external credentials, optional web-search dependencies, disabled MCP
examples, or misleading success text distracting from the `chat` demo.

Implementation must preserve real-provider defaults and existing user-edited
configuration.
