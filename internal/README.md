# Internal (Auriga)

Scripts and Docker layout here are for **Auriga team QA / Cloud Run playground** only. They are tracked in git for the private repo; **remove this directory (or re-ignore it) before open-sourcing** so public consumers are not pointed at Auriga defaults.

Public baseline: root [`docker-compose.yml`](../docker-compose.yml), [`docker/Dockerfile`](../docker/Dockerfile), and [`docs/cloud-deployment-design.md`](../docs/cloud-deployment-design.md) Step 3.
