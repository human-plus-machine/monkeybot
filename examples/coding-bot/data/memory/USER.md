# User Preferences

- Prefer Markdown with fenced code blocks tagged by language.
- Cite file paths and line numbers when referencing source code.
- Numbered steps for multi-step fixes; keep each step single-purpose.
- Avoid redundant narration in code comments — only explain non-obvious intent.
