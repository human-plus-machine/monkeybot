description: "Past-event query triggers mempalace search via run_command"
user_message: "What did we do yesterday?"
scripted_turns:
  - - type: tool_call
      call_id: c1
      name: run_command
      args:
        argv: ["mempalace", "search", "yesterday"]
    - type: done
  - - type: text_delta
      text: "Yesterday we..."
    - type: done
assertions:
  tool_categories_used:
    - shell
  turn_completed: true
  max_turns: 3
